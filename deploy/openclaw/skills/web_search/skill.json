{
  "id": "web_search",
  "name": "网页搜索",
  "description": "联网搜索给定关键词，返回相关网页摘要。本地 OpenClaw Skill。",
  "parameters": {
    "type": "object",
    "properties": {
      "query": { "type": "string", "description": "搜索关键词" },
      "top_k": { "type": "integer", "description": "返回条数", "default": 5 }
    },
    "required": ["query"]
  }
}
